# personal study notes — not part of the deliverable
NOTES.md

__pycache__/
*.pyc

# The 1-year dataset ships as Parquet (25.8 MB, committed) so a deployment
# has data without a fetch step. The 57 MB CSV it was built from is just an
# intermediate — regenerate it with the fetch command in README.md.
data/sol_1m_1y.csv

# local tool settings
.claude/

# scratch charts from a run at a different bin step, kept locally for
# comparison and deliberately not part of the repo
outputs/testbin20/

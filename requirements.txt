# What the deployed app needs, and nothing else.
streamlit==1.60.0
# The Run history page reads results/runs.csv with pandas. Streamlit already
# depends on pandas, but this module imports it directly, so it is declared.
pandas>=2.0
# Run it yourself is the one page that runs the engine, so the server needs
# what a run touches: pyarrow to read the Parquet dataset, and matplotlib
# and numpy to draw a run's charts. charts.py imports numpy directly rather
# than relying on it arriving with matplotlib.
pyarrow>=14.0
matplotlib>=3.8
numpy>=1.26
